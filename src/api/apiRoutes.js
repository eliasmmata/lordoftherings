const BASE_URL = "https://the-lord-of-the-rings.herokuapp.com";

const CHARACTERS = `${BASE_URL}/characters`;
const LOCATIONS = `${BASE_URL}/locations`;
const ITEMS = `${BASE_URL}/items`;
const WEAPONS = `${BASE_URL}/weapons`;

export {
    BASE_URL,
    CHARACTERS,
    LOCATIONS,
    ITEMS,
    WEAPONS
}